<resources>

    <declare-styleable name="atts_tick">
        <attr name="radius"         format="dimension"></attr> <!-- 默认圆半径 -->
        <attr name="tick_color"     format="color"></attr>  <!-- 默认对勾颜色 -->
        <attr name="circle_color"   format="color"></attr>  <!-- 默认选中情况下圆填充色 -->
    </declare-styleable>

</resources>
